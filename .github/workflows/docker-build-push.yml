# Build and Push Docker Image
#
# This workflow builds the Docker image for AMD64 architecture and pushes to Docker Hub.
#
# Build Configuration:
#   - Platform: linux/amd64 only (ARM64 disabled due to cross-compilation issues)
#   - Cache: Disabled to ensure clean, reliable builds
#   - Triggers: Push to main branch, version tags, and pull requests
#
# Known Issues Resolved:
#   - Multi-platform builds (amd64+arm64) with buildx produced non-functional binaries
#   - GitHub Actions cache was corrupting Rust builds
#   - Solution: Single platform (amd64), no cache
#
# See _docs/DOCKER_IMAGES.md for more details.

name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

env:
  REGISTRY: docker.io
  IMAGE_NAME: maanstr/idrac-fan-controller-rust

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for PRs
        if: github.event_name == 'pull_request'
        id: meta-pr
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=beta

      - name: Extract metadata (tags, labels) for main branch
        if: github.event_name != 'pull_request'
        id: meta-main
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image for PRs (:beta tag)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          # IMPORTANT: AMD64 only - do not add ARM64 (causes broken binaries)
          # See _docs/DOCKER_IMAGES.md for why multi-platform builds are disabled
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta-pr.outputs.tags }}
          labels: ${{ steps.meta-pr.outputs.labels }}
          # Disable cache temporarily to match working local build
          # cache-from: type=gha
          # cache-to: type=gha,mode=max

      - name: Build and push Docker image for main (:latest tag)
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          # IMPORTANT: AMD64 only - do not add ARM64 (causes broken binaries)
          # See _docs/DOCKER_IMAGES.md for why multi-platform builds are disabled
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta-main.outputs.tags }}
          labels: ${{ steps.meta-main.outputs.labels }}
          # Disable cache temporarily to match working local build
          # cache-from: type=gha
          # cache-to: type=gha,mode=max

      # Docker Hub Description sync disabled - update manually on Docker Hub if needed
      # Uncomment below and ensure DOCKERHUB_TOKEN has Read & Write permissions
      # - name: Docker Hub Description
      #   if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
      #   uses: peter-evans/dockerhub-description@v4
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      #     repository: ${{ env.IMAGE_NAME }}
      #     short-description: "Dell iDRAC Fan Controller with Exponential Curve - Rust Edition"
      #     readme-filepath: ./README.md
